---
description: Show task status across all states
---

Show the current task board.

```bash
python orchestrator/agent-team.py status
```

Summarize the counts per state and list any non-empty states with their task
files.
