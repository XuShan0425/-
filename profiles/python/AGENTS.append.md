# Python Project Profile

- Prefer the repository's existing virtual environment or dependency manager.
- Run `ruff check .`, `mypy .`, `pytest`, or `tox` when configured and available.
- Do not commit virtual environments, caches, coverage output, or local secrets.
- Keep dependency changes explicit and justified in the task or PR.
- Treat failing tests and type checks as blocking.
