# Node Project Profile

- Prefer the package manager implied by the lockfile.
- Run `lint`, `typecheck`, and `test` scripts when they exist.
- Do not change lockfiles unless dependency changes are required by the task.
- Keep generated build outputs out of commits unless the repository already versions them.
- For TypeScript projects, treat typecheck failures as blocking.
