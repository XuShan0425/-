# Generic Project Profile

- Use the repository's existing build, test, and lint commands.
- Prefer the smallest task split that can be reviewed independently.
- Add verification commands to every non-trivial task file.
- Do not introduce new frameworks or services without explicit task scope.
